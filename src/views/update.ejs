<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Update user</title>
</head>

<body>
    <div class="update-user-container">
        <form action="/update-user/<%= userUpdate.id %>" method="POST">
            <div>
                <label>First Name</label>
                <input name="firstName" value="<%= userUpdate.firstName %>" />
            </div>
            <div>
                <label>Last Name</label>
                <input name="lastName" value="<%= userUpdate.lastName %>" />
            </div>
            <div>
                <label>Email</label>
                <input name="email" value="<%= userUpdate.email %>" />
            </div>
            <div>
                <label>Address</label>
                <input name="address" value="<%= userUpdate.address %>" />
            </div>
            <input type="text" value="<%= userUpdate.id %>" hidden name="id" />
            <button class="btn-save" type="submit">Save</button>
        </form>
    </div>
</body>

</html>